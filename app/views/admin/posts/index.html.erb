<div class="container mt-2">
  <div class="row">
    <div class="col-md-8">
      <h3>お知らせ投稿一覧</h3>
      <p>避難場所をクリックすると詳細画面へ移動します。</p>
      <%= link_to new_admin_post_path, class: 'text-dark' do %>
        <i class="fas fa-plus">新規投稿</i>
      <% end %>
    </div>

      <table border="1" cellpadding="5" style="width:100%">
        <thead>
          <tr>
            <th class="table-secondary">避難場所</th>
            <th class="table-secondary">説明</th>
            <th class="table-secondary">住所</th>
            <th class="table-secondary">登録日</th>
          </tr>
        </thead>
      <% @posts.each do |post| %>
        <tbody>
            <td>
              <%= link_to admin_post_path(post), class: 'text-info' do %>
                <%= post.name %>
              <% end %>
            </td>
            <td><%= post.introduction %></td>
            <td><%= post.address %></td>
            <td><%= post.created_at.strftime('%Y/%m/%d') %></td>
          </tr>
        </tbody>
      <% end %>
      </table>
  </div>
  <%= paginate @posts %>
</div>