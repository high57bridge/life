<div class="container mt-2">
  <div class="row">
    <div class="col-md-8">
      <h3>ご意見・ご質問一覧</h3>
      <p>顧客の氏名をクリックすると詳細確認画面へ遷移します。</p>
    </div>
        <table border="1" cellpadding="5" style="width:100%">
          <thead>
            <tr>
              <th class="table-secondary" style="width:15%">お名前</th>
              <th class="table-secondary" style="width:20%">メールアドレス</th>
              <th class="table-secondary" style="width:40%">お問い合わせ内容</th>
              <th class="table-secondary" style="width:10%">受信日</th>
              <th class="table-secondary" style="width:25%">確認ステータス</th>
            </tr>
          </thead>
        <% @homes.each do |home| %>
          <tbody>
            <tr>
              <td>
                <%= link_to admin_home_path(home), class: "text-dark" do %>
                  <%= home.name %>
                <% end %>
              </td>
              <td><%= home.email %></td>
              <td><%= home.opinion %></td>
              <td><%= home.created_at.strftime('%Y/%m/%d') %></td>
              <td>
                <% if home.is_active %>
                  <div class="text-primary">未確認</div>
                <% else %>
                  <div class="text-danger">確認済</div>
                <% end %>
              </td>
            </tr>
          </tbody>
        <% end %>
        </table>
  </div>
</div>