<h2>検索結果</h2>

<table class="table table-hover table-inverse">
  <% @range == "Post" %>
    <!--検索対象モデルがPostの時 -->
      <% @posts.each do |post| %>
        <tr>
          <td><%= post.name %></td>
          <td><%= post.introduction %></td>
        </tr>
      <% end %>
</table>